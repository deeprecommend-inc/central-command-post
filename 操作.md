# SNS Orchestrator 操作マニュアル

## 目次
1. [システム概要](#システム概要)
2. [重要な変更点](#重要な変更点)
3. [起動・停止方法](#起動停止方法)
4. [アクセス方法](#アクセス方法)
5. [基本操作](#基本操作)
6. [ブラウザ自動化テスト](#ブラウザ自動化テスト)
7. [API使用方法](#api使用方法)
8. [トラブルシューティング](#トラブルシューティング)

---

## システム概要

SNS Orchestratorは、YouTube、X（Twitter）、Instagram、TikTokなどのSNSプラットフォームを統合管理するシステムです。

### 主な機能
- **アカウント管理**: 複数のSNSアカウントを一元管理
- **ペルソナ管理**: アカウントに個性・行動パターンを設定
- **プロキシ管理**: IP/プロキシの品質管理
- **タスク実行**: 投稿、いいね、コメントなどの自動化
- **アカウント生成**: 大規模なアカウント自動生成（10万〜100万アカウント対応）
- **ブラウザ自動化**: Playwright使用、API不要の完全ブラウザ操作

### システム構成
- **フロントエンド**: Next.js (ポート: 3006)
- **バックエンドAPI**: FastAPI (ポート: 8006)
- **データベース**: PostgreSQL (ポート: 5433)
- **キャッシュ**: Redis (ポート: 6380)
- **ワーカー**: タスク実行エンジン
- **ブラウザ自動化**: Playwright + Mulogin + BrightData

---

## 重要な変更点

### ⚠️ API連携からブラウザ自動化へ移行

**変更理由**:
- API契約が不要（リスク低減）
- プラットフォームからの検知回避
- より柔軟な操作が可能

**実装済み機能**:

#### YouTube (完全ブラウザ操作)
- ✅ Gmail アカウント生成（電話番号認証回避）
- ✅ ログイン
- ✅ 動画アップロード
- ✅ いいね
- ✅ コメント
- ✅ チャンネル登録
- ✅ 検索
- ✅ チャンネル情報取得

#### X (Twitter)
- ✅ ログイン
- ✅ ツイート投稿
- ✅ いいね
- ✅ リツイート
- ✅ 返信
- ✅ フォロー
- ✅ 検索
- ✅ ユーザー情報取得

#### Instagram
- ✅ ログイン
- ✅ 写真投稿
- ✅ いいね
- ✅ コメント
- ✅ フォロー
- ✅ ハッシュタグ検索
- ✅ DM送信
- ✅ ユーザー情報取得

#### TikTok
- ✅ アカウント登録（実装済み）
- 🔄 その他の操作（今後実装）

### 大規模アカウント生成対応

- **対応規模**: 10,000 〜 1,000,000 アカウント
- **同時生成数**: 10 → 50 に増強
- **バッチ処理**: 100〜1000アカウント/バッチ
- **プロキシローテーション**: 1アカウントにつき1IP（固定セッション）
- **ブラウザ指紋**: Mulogin連携で個別指紋

---

## 起動・停止方法

### 初回セットアップ

```bash
# 1. リポジトリのルートディレクトリに移動
cd /usr/src/script/sns-agents

# 2. 環境変数ファイルを作成（必要に応じて編集）
cp backend/.env.example backend/.env

# 3. Docker コンテナをビルド＆起動
docker-compose up -d --build
```

### 起動

```bash
# すべてのコンテナを起動
docker-compose up -d

# 特定のサービスのみ起動
docker-compose up -d backend frontend
```

### 停止

```bash
# すべてのコンテナを停止
docker-compose down

# データを保持したまま停止
docker-compose stop

# データを削除して完全停止
docker-compose down -v
```

### 再起動

```bash
# すべてのコンテナを再起動
docker-compose restart

# 特定のサービスのみ再起動
docker-compose restart backend
```

### ログ確認

```bash
# すべてのログを表示
docker-compose logs -f

# 特定のサービスのログを表示
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f worker
```

### コンテナ状態確認

```bash
# 実行中のコンテナを確認
docker-compose ps

# ヘルスチェック
curl http://localhost:8006/health
```

---

## アクセス方法

### フロントエンド（Web UI）
```
http://localhost:3006
```

### バックエンドAPI
```
http://localhost:8006
```

### API ドキュメント（Swagger UI）
```
http://localhost:8006/docs
```

### データベース接続
```
ホスト: localhost
ポート: 5433
ユーザー: sns_user
パスワード: sns_password
データベース: sns_orchestrator
```

### Redis接続
```
ホスト: localhost
ポート: 6380
```

---

## 基本操作

### 1. ダッシュボード

トップページ（`http://localhost:3006`）でシステム全体の状況を確認できます。

- アカウント数
- 実行中のタスク数
- 今日の実行数
- プロキシ数

### 2. アカウント管理

**アカウント一覧**: `/accounts`
- 登録済みアカウントを一覧表示
- プラットフォーム、ステータスでフィルタリング
- アカウント詳細の表示・編集

**新規アカウント登録**: `/accounts/new`
1. プラットフォームを選択（YouTube / X / Instagram / TikTok）
2. ユーザー名を入力
3. 認証情報を入力（OAuth or ユーザー名/パスワード）
4. 「登録」ボタンをクリック

### 3. ペルソナ管理

**ペルソナ一覧**: `/personas`
- ペルソナ（アカウントの人格）を管理
- 年齢層、性別、興味、行動パターンなどを設定

**新規ペルソナ作成**: `/personas/new`
1. ペルソナ名を入力
2. タイプを選択（個人/ビジネス/インフルエンサー等）
3. デモグラフィック情報を入力
   - 年齢層
   - 性別
   - 地域（国・都市）
   - タイムゾーン
   - 言語
4. 興味・関心を入力
5. 行動パターンを設定
   - アクティブ時間帯
   - 投稿頻度
   - インタラクションスタイル
6. ブラウザ指紋設定（Mulogin連携時）
7. 「作成」ボタンをクリック

### 4. プロキシ管理

**プロキシ一覧**: `/proxy-ips`
- 登録済みプロキシの一覧表示
- 品質別フィルタリング（excellent/good/fair/poor/untested）

**新規プロキシ追加**: `/proxy-ips` 「新規追加」ボタン
1. IPアドレスを入力
2. ポートを入力
3. プロキシタイプを選択（HTTP/HTTPS/SOCKS5）
4. 認証情報を入力（必要な場合）
5. 「追加」ボタンをクリック

**一括インポート**:
1. テキストエリアにプロキシリストを貼り付け
   ```
   192.168.1.1:8080
   user:pass@192.168.1.2:8080
   http://192.168.1.3:8080
   socks5://192.168.1.4:1080
   ```
2. 「抽出」ボタンをクリック
3. プレビューを確認
4. 「一括追加」ボタンをクリック

### 5. タスク実行

**Run作成**: `/runs/create`
1. Run名を入力
2. アカウントを選択
3. アクションタイプを選択
   - post: 投稿
   - like: いいね
   - comment: コメント
   - follow: フォロー
   - subscribe: チャンネル登録
4. アクション設定を入力
   - 投稿の場合: テキスト、メディア
   - いいねの場合: 対象URL
   - コメントの場合: 対象URL、コメントテキスト
5. スケジュール設定
   - 即座に実行 or スケジュール実行
   - 実行日時を設定
6. Bot検知回避設定
   - プロキシリストを貼り付け
   - 遅延時間を設定
   - ランダム化オプション
7. 「作成」ボタンをクリック

**Run一覧**: `/runs`
- 作成済みRunの一覧表示
- ステータス確認（pending/running/completed/failed）
- Run詳細の表示
- Run削除

### 6. アカウント生成

**生成タスク作成**: API経由（現在UIは未実装）

```bash
curl -X POST http://localhost:8006/account-generation/tasks \
  -H "Content-Type: application/json" \
  -d '{
    "platform": "youtube",
    "target_count": 100,
    "persona_id": 1,
    "username_pattern": "user_{}",
    "email_domain": "gmail.com",
    "use_brightdata": true,
    "use_mulogin": true,
    "batch_size": 100,
    "headless": true
  }'
```

**生成タスク一覧**: API経由
```bash
curl http://localhost:8006/account-generation/tasks
```

**生成タスク開始**: API経由
```bash
curl -X POST http://localhost:8006/account-generation/tasks/{task_id}/start
```

---

## ブラウザ自動化テスト

### テストスクリプトの使用方法

プロジェクトルートディレクトリにある `test_automation.py` を使用して、各プラットフォームの自動化機能をテストできます。

### 前提条件

```bash
# Playwrightブラウザのインストール（初回のみ）
pip install playwright
playwright install chromium
```

### Gmail生成テスト

電話番号認証を回避してGmailアカウントを作成します。

```bash
python test_automation.py --platform youtube --action test_gmail
```

**実行内容**:
- ランダムな名前・ユーザー名・パスワードを生成
- モバイルUser-Agentを使用
- シークレットモードでGmail作成
- 電話番号スキップ

**成功時の出力例**:
```
✓ SUCCESS: Gmail account created!
  Email: johnsmith1234@gmail.com
  Password: Xy9#mK2@pL5&qR8$
```

### YouTubeテスト

#### ログインテスト

```bash
python test_automation.py \
  --platform youtube \
  --action login \
  --email your_email@gmail.com \
  --password your_password
```

#### いいねテスト

```bash
python test_automation.py \
  --platform youtube \
  --action like \
  --email your_email@gmail.com \
  --password your_password \
  --video-url "https://youtube.com/watch?v=xxxxx"
```

#### コメントテスト

```bash
python test_automation.py \
  --platform youtube \
  --action comment \
  --email your_email@gmail.com \
  --password your_password \
  --video-url "https://youtube.com/watch?v=xxxxx" \
  --comment "Great video!"
```

#### チャンネル登録テスト

```bash
python test_automation.py \
  --platform youtube \
  --action subscribe \
  --email your_email@gmail.com \
  --password your_password \
  --channel-url "https://youtube.com/@channelname"
```

### X (Twitter) テスト

#### ログインテスト

```bash
python test_automation.py \
  --platform x \
  --action login \
  --username your_username \
  --password your_password
```

#### ツイートテスト

```bash
python test_automation.py \
  --platform x \
  --action post \
  --username your_username \
  --password your_password \
  --text "Hello from automation!"
```

### Instagram テスト

#### ログインテスト

```bash
python test_automation.py \
  --platform instagram \
  --action login \
  --username your_username \
  --password your_password
```

### プロキシテスト

BrightDataプロキシの動作確認:

```bash
python test_automation.py --platform proxy --action test
```

**前提**: `backend/.env` に BrightData の認証情報が設定されていること

```env
BRIGHTDATA_USERNAME=your_username
BRIGHTDATA_PASSWORD=your_password
BRIGHTDATA_ZONE=residential
```

### 大規模アカウント生成テスト

#### 小規模テスト（10アカウント）

```bash
curl -X POST http://localhost:8006/account-generation/tasks \
  -H "Content-Type: application/json" \
  -d '{
    "platform": "youtube",
    "target_count": 10,
    "username_pattern": "testuser_{}",
    "email_domain": "gmail.com",
    "use_brightdata": true,
    "use_mulogin": false,
    "batch_size": 10,
    "headless": true
  }'
```

#### 中規模テスト（1,000アカウント）

```bash
curl -X POST http://localhost:8006/account-generation/tasks \
  -H "Content-Type: application/json" \
  -d '{
    "platform": "youtube",
    "target_count": 1000,
    "persona_id": 1,
    "use_brightdata": true,
    "use_mulogin": true,
    "batch_size": 100,
    "headless": true
  }'
```

#### 大規模テスト（100,000アカウント）

```bash
curl -X POST http://localhost:8006/account-generation/tasks \
  -H "Content-Type: application/json" \
  -d '{
    "platform": "youtube",
    "target_count": 100000,
    "persona_id": 1,
    "use_brightdata": true,
    "use_mulogin": true,
    "batch_size": 1000,
    "headless": true
  }'
```

**注意事項**:
- 大規模生成時は十分なプロキシを用意してください（1アカウントにつき1-2個のIP）
- BrightData/Muloginの契約プランを確認してください
- 生成速度は同時生成数（max_concurrent）とバッチサイズで調整可能

### タスク監視

```bash
# タスク一覧
curl http://localhost:8006/account-generation/tasks

# 特定タスクの詳細
curl http://localhost:8006/account-generation/tasks/1

# タスク開始
curl -X POST http://localhost:8006/account-generation/tasks/1/start

# タスクキャンセル
curl -X POST http://localhost:8006/account-generation/tasks/1/cancel
```

### デバッグモード

ヘッドレスモードを無効にしてブラウザを表示:

```python
# test_automation.py内で headless=False に変更されています
# ブラウザの動作を目で確認できます
```

---

## API使用方法

### 認証

現在は認証なしでAPIを使用できます。本番環境では認証を実装してください。

### 主要エンドポイント

#### アカウント管理

**アカウント一覧取得**
```bash
GET /accounts?platform=youtube&status=active&limit=50
```

**アカウント詳細取得**
```bash
GET /accounts/{account_id}
```

**アカウント作成**
```bash
POST /accounts
{
  "platform": "youtube",
  "username": "test_user",
  "email": "test@example.com",
  "auth_type": "oauth",
  "access_token": "xxx"
}
```

**アカウント更新**
```bash
PUT /accounts/{account_id}
{
  "status": "suspended"
}
```

**アカウント削除**
```bash
DELETE /accounts/{account_id}
```

#### ペルソナ管理

**ペルソナ一覧取得**
```bash
GET /personas?limit=50
```

**ペルソナ作成**
```bash
POST /personas
{
  "name": "Tech Enthusiast",
  "persona_type": "individual",
  "age_range": "25-34",
  "gender": "male",
  "location_country": "US",
  "location_city": "San Francisco",
  "interests": ["technology", "programming", "AI"],
  "activity_hours": {"start": 9, "end": 22}
}
```

#### プロキシ管理

**プロキシ一覧取得**
```bash
GET /proxy-ips?quality=excellent&limit=100
```

**プロキシ追加**
```bash
POST /proxy-ips
{
  "ip_address": "192.168.1.1",
  "port": 8080,
  "proxy_type": "http",
  "username": "user",
  "password": "pass"
}
```

**プロキシ品質チェック**
```bash
POST /proxy-ips/{proxy_id}/check
```

#### Run管理

**Run作成**
```bash
POST /runs
{
  "name": "Test Post Run",
  "account_ids": [1, 2, 3],
  "action_type": "post",
  "action_config": {
    "text": "Hello World!",
    "media_urls": []
  },
  "schedule_config": {
    "execute_at": "2025-01-15T10:00:00Z"
  }
}
```

**Run一覧取得**
```bash
GET /runs?status=pending&limit=50
```

**Run実行**
```bash
POST /runs/{run_id}/execute
```

#### アカウント生成

**生成タスク作成**
```bash
POST /account-generation/tasks
{
  "platform": "youtube",
  "target_count": 1000,
  "persona_id": 1,
  "use_brightdata": true,
  "use_mulogin": true,
  "batch_size": 100
}
```

**生成タスク開始**
```bash
POST /account-generation/tasks/{task_id}/start
```

**生成タスクキャンセル**
```bash
POST /account-generation/tasks/{task_id}/cancel
```

### APIドキュメント

詳細なAPIドキュメントは Swagger UI で確認できます:
```
http://localhost:8006/docs
```

---

## トラブルシューティング

### コンテナが起動しない

**症状**: `docker-compose up -d` でエラーが発生

**解決方法**:
1. ログを確認
   ```bash
   docker-compose logs backend
   docker-compose logs frontend
   ```

2. ポートが使用中の場合
   ```bash
   # ポート使用状況を確認
   lsof -i :3006
   lsof -i :8006
   lsof -i :5433

   # 使用中のプロセスを停止するか、docker-compose.ymlのポートを変更
   ```

3. ビルドし直す
   ```bash
   docker-compose down -v
   docker-compose up -d --build
   ```

### バックエンドAPIが応答しない

**症状**: `curl http://localhost:8006/health` が失敗

**解決方法**:
1. バックエンドのログを確認
   ```bash
   docker-compose logs -f backend
   ```

2. データベース接続を確認
   ```bash
   docker-compose exec db psql -U sns_user -d sns_orchestrator
   # 接続できれば \dt でテーブル一覧を確認
   ```

3. 環境変数を確認
   ```bash
   docker-compose exec backend env | grep DATABASE
   ```

4. バックエンドを再起動
   ```bash
   docker-compose restart backend
   ```

### フロントエンドが表示されない

**症状**: `http://localhost:3006` にアクセスできない

**解決方法**:
1. フロントエンドのログを確認
   ```bash
   docker-compose logs -f frontend
   ```

2. バックエンドAPIへの接続を確認
   - フロントエンドは `http://localhost:8006` に接続します
   - バックエンドが起動していることを確認

3. ブラウザのキャッシュをクリア

4. フロントエンドを再起動
   ```bash
   docker-compose restart frontend
   ```

### データベースマイグレーションエラー

**症状**: テーブルが存在しない、スキーマエラー

**解決方法**:
1. マイグレーションを実行
   ```bash
   docker-compose exec backend alembic upgrade head
   ```

2. マイグレーション履歴を確認
   ```bash
   docker-compose exec backend alembic current
   docker-compose exec backend alembic history
   ```

3. データベースをリセット（注意: データが削除されます）
   ```bash
   docker-compose down -v
   docker-compose up -d
   ```

### プロキシ接続エラー

**症状**: プロキシ経由の接続が失敗

**解決方法**:
1. プロキシの品質チェックを実行
   - Web UI: `/proxy-ips` → プロキシを選択 → 「チェック」ボタン

2. プロキシ形式を確認
   - サポート形式: `IP:PORT`, `USER:PASS@IP:PORT`, `http://IP:PORT`, `socks5://IP:PORT`

3. プロキシの到達性を確認
   ```bash
   curl -x http://USER:PASS@PROXY_IP:PORT https://ifconfig.me
   ```

### Mulogin/BrightData連携エラー

**症状**: アカウント生成が失敗

**解決方法**:
1. API鍵を確認
   ```bash
   docker-compose exec backend env | grep MULOGIN
   docker-compose exec backend env | grep BRIGHTDATA
   ```

2. `.env` ファイルに正しいAPI鍵を設定
   ```
   MULOGIN_API_URL=http://127.0.0.1:35000/api/v1
   MULOGIN_API_KEY=your_api_key

   BRIGHTDATA_USERNAME=your_username
   BRIGHTDATA_PASSWORD=your_password
   BRIGHTDATA_ZONE=residential
   ```

3. バックエンドを再起動
   ```bash
   docker-compose restart backend
   ```

### ワーカーがタスクを実行しない

**症状**: Runが pending のまま

**解決方法**:
1. ワーカーのログを確認
   ```bash
   docker-compose logs -f worker
   ```

2. Redisの接続を確認
   ```bash
   docker-compose exec redis redis-cli ping
   # "PONG" と返ってくればOK
   ```

3. ワーカーを再起動
   ```bash
   docker-compose restart worker
   ```

### ディスク容量不足

**症状**: コンテナが停止、ログが出力されない

**解決方法**:
1. ディスク使用量を確認
   ```bash
   df -h
   docker system df
   ```

2. 不要なイメージ・コンテナを削除
   ```bash
   docker system prune -a
   ```

3. ログファイルを削除
   ```bash
   docker-compose logs --tail=0 backend > /dev/null
   ```

---

## 設定ファイル

### バックエンド環境変数 (`backend/.env`)

```env
# データベース
DATABASE_URL=postgresql+asyncpg://sns_user:sns_password@db:5432/sns_orchestrator

# Redis
REDIS_URL=redis://redis:6379/0

# API鍵
YOUTUBE_API_KEY=your_youtube_api_key
YOUTUBE_CLIENT_ID=your_youtube_client_id
YOUTUBE_CLIENT_SECRET=your_youtube_client_secret

X_API_KEY=your_x_api_key
X_API_SECRET=your_x_api_secret

INSTAGRAM_CLIENT_ID=your_instagram_client_id
INSTAGRAM_CLIENT_SECRET=your_instagram_client_secret

TIKTOK_CLIENT_KEY=your_tiktok_client_key
TIKTOK_CLIENT_SECRET=your_tiktok_client_secret

# Mulogin
MULOGIN_API_URL=http://127.0.0.1:35000/api/v1
MULOGIN_API_KEY=your_mulogin_api_key

# BrightData
BRIGHTDATA_USERNAME=your_brightdata_username
BRIGHTDATA_PASSWORD=your_brightdata_password
BRIGHTDATA_ZONE=residential

# その他
SECRET_KEY=your_secret_key_here
ENVIRONMENT=development
```

### Docker Compose設定 (`docker-compose.yml`)

ポート変更例:
```yaml
services:
  frontend:
    ports:
      - "3007:3006"  # ホスト側のポートを3007に変更

  backend:
    ports:
      - "8007:8006"  # ホスト側のポートを8007に変更
```

---

## よくある質問（FAQ）

### Q1: 同時に複数のRunを実行できますか？

A: はい、ワーカーが並列でタスクを実行します。同時実行数はワーカーの設定で変更可能です。

### Q2: アカウントを大量に生成する際の注意点は？

A:
- プロキシを十分に用意してください（1アカウントにつき1-2個のIP）
- Muloginのプロファイル容量を確認してください
- バッチサイズを適切に設定してください（推奨: 100-1000）
- 生成速度に注意してください（プラットフォームの制限）

### Q3: プロキシの品質判定基準は？

A:
- **excellent**: レスポンス < 300ms、成功率 > 95%
- **good**: レスポンス < 500ms、成功率 > 90%
- **fair**: レスポンス < 1000ms、成功率 > 80%
- **poor**: レスポンス > 1000ms、成功率 < 80%
- **untested**: 未チェック

### Q4: ペルソナとアカウントの関係は？

A: ペルソナはアカウントの「人格設定」です。1つのペルソナに複数のアカウントを紐付けることで、同じ行動パターンを持つアカウント群を管理できます。

### Q5: バックアップ方法は？

A: データベースとRedisのバックアップ:
```bash
# PostgreSQLバックアップ
docker-compose exec db pg_dump -U sns_user sns_orchestrator > backup.sql

# リストア
cat backup.sql | docker-compose exec -T db psql -U sns_user sns_orchestrator
```

---

## サポート

問題が解決しない場合:
1. ログを確認: `docker-compose logs -f`
2. GitHub Issuesで報告
3. ドキュメントを再確認: `http://localhost:8006/docs`

---

最終更新: 2025-01-12
